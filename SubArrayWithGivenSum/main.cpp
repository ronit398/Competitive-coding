#include <iostream>
using namespace std;
int main()
{
    int T;
    cout<<"Enter the number of test case: ";
    cin>>T;
    while(T--)
    {
        int N,i;
        cout<<"Enter the size of array: ";
        cin>>N;
        int arr[N];
        cout<<"Enter the elements in array: ";
        for(i=0,i<N;i++)

    }
    return 0;
}
